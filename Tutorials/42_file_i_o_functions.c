// If you want to run this file then first uncomment that section
// Don't uncomment more than 1 section at same time

#include <stdio.h>

int main(int argc, char const *argv[])
{
    FILE *ptr = NULL;
    char string[34] = "\nThis is a test string";

    // Reading a file
    // ptr = fopen("42_file_i_o_functions_test.txt", "r");
    // fscanf(ptr, "%s", string);
    // printf("The content of this file is\n%s", string);

    // Writing a file
    // ptr = fopen("42_file_i_o_functions_test.txt", "w");
    // fprintf(ptr, "%s", string);
    
    // Appending a file
    // ptr = fopen("42_file_i_o_functions_test.txt", "a");
    // fprintf(ptr, "%s", string);

    return 0;
}